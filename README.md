# To-Find-LCM
To find the Least Common Multiple (LCM) of two numbers, you can use the formula:  LCM(a, b) = (a * b) / GCD(a, b)  Where GCD is the Greatest Common Divisor. The LCM is the smallest number that is a multiple of both a and b.
